{
  "name": "card-memory",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "start": "chmod +x node_modules/.bin/vite && vite preview",
    "dev": "vite",
    "build": "chmod +x node_modules/.bin/vite && vite build",
    "preview": "vite preview",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "vite": "^5.2.0"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "ethers": "^6.13.4",
    "html2canvas": "^1.4.1",
    "noisejs": "^2.1.0",
    "phaser": "^3.80.1",
    "web3": "^4.13.0"
  }
}
